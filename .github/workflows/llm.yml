name: Run Tiny LLM (Fixed)

on:
  workflow_dispatch:

jobs:
  llm:
    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git build-essential cmake wget

      - name: Clone llama.cpp
        run: |
          git clone https://github.com/ggml-org/llama.cpp.git

      - name: Build llama.cpp
        run: |
          cd llama.cpp
          cmake -B build
          cmake --build build -j$(nproc)

      - name: Download model
        run: |
          wget -O model.gguf https://huggingface.co/QuantLLM/SmolLM2-135M-GGUF/resolve/main/SmolLM2-135M.Q4_K_M.gguf

      - name: Run model
        run: |
          PROMPT="<|im_start|>user\nHello\n<|im_end|>\n<|im_start|>assistant\n"

          ./llama.cpp/build/bin/llama-cli \
            -m model.gguf \
            -p "$PROMPT" \
            -n 32 \
            --temp 0.7 \
            --top-p 0.9 \
            --repeat-penalty 1.2 \
            --log-disable